import React from "react";
import { Alert } from "react-bootstrap";

const ErrorAlert = () => {
  return (
    <Alert variant="danger">
      <Alert.Heading>Ajeje problem</Alert.Heading>
    </Alert>
  );
};

export default ErrorAlert;
